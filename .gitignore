# Secrets — never commit
.env
tokens.js

# infrared_result.json is committed so GitHub Pages works out of the box.
# Regenerate locally with: npm run mock

# Dependencies
node_modules/

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
